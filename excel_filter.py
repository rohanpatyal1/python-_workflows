import pandas as pd\nimport os\n\n# Set the input and output directories\ninput_folder = 'input_excel'\noutput_folder = 'output_excel'\n\n# Read the Excel file\ninput_file = os.path.join(input_folder, 'input_file.xlsx')  # Change 'input_file.xlsx' to your actual input file name\ndata = pd.read_excel(input_file)\n\n# Apply filtering criteria\n# Replace 'column_name' and 'criteria' with actual column name and the criteria you want to filter by\nfiltered_data = data[data['column_name'] == 'criteria']\n\n# Write the output to a new Excel file\noutput_file = os.path.join(output_folder, 'filtered_output.xlsx')\nfiltered_data.to_excel(output_file, index=False)\n\nprint("Data filtering complete. Output saved to:", output_file)